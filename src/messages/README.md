# `src/messages/`

The only place user-facing copy exists.

## Rules

- Every string is keyed. This is not an i18n framework — it is what makes i18n a later data exercise instead of a rewrite (§4.2).
- No currency symbol, date format or country name belongs here.

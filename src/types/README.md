# `src/types/`

Domain types and generated Supabase types.

## Rules

- `database.ts` is generated by `npm run db:types` (T02) and committed.
- Hand-written domain types live alongside it, never inside `app/`.

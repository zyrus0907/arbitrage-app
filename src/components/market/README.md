# `src/components/market/`

MarketSwitcher, CurrencyDisplay.

## Rules

- `MarketSwitcher` renders only when more than one market is live.
- No currency symbol is hard-coded; formatting is `Intl.NumberFormat`.

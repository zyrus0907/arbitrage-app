# `src/components/deals/`

DealCard, DealDetail, ScoreBreakdown, AssumptionsPanel, UnitRecommendation, RiskFlags.

## Rules

- `DealCard` (locked) must have **no props capable of carrying identity** — no title, image, marketplace external id, retailer name, GTIN (AC8.2).

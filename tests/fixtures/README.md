# `tests/fixtures/`

Test data: hand-worked pricing cases, provider payloads, sample CSVs.

## Rules

- Hand-worked expected values must be derived independently of the implementation. A case produced by running the code and pasting its output does not count (T15).
